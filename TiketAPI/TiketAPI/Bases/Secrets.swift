public enum Secrets {
    
    public enum Api {
        public enum Client {
            public static let production = ""
            public static let staging = ""
        }
        
        public enum Endpoint {
            public static let production = "api.tiket.com"
            public static let staging = "api-sandbox.tiket.com"
        }
    }
    
    public enum WebEndPoint {
        public static let production = "www.tiket.com"
        public static let staging = "www.tiket.com"
    }
}

